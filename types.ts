export interface NavLink {
  label: string;
  href: string;
}

export interface BenefitItem {
  title: string;
  description: string;
}

export interface AudienceItem {
  title: string;
  active: boolean;
}